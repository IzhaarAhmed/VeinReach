{
  "permissions": {
    "allow": [
      "Bash(npm --version)",
      "Bash(git --version)",
      "PowerShell($count=0; while \\($count -lt 30\\) { Start-Sleep -Milliseconds 1000; $count++; try { $h = Invoke-RestMethod -Uri \"http://localhost:5050/api/v1/health\" -TimeoutSec 2 -ErrorAction Stop; if \\($h.success\\) { \"BACKEND_READY after ${count}s\"; break } } catch {} }; if \\($count -ge 30\\) { \"backend not ready after 30s\" })"
    ]
  },
  "enabledMcpjsonServers": [
    "21st"
  ],
  "enableAllProjectMcpServers": true
}
